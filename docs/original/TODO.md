# TODO

- RAG v2: evaluate a dedicated code/technical embedding model for indexing and retrieval instead of the current lightweight TF-IDF baseline; keep hybrid search and compare result quality on a real project.

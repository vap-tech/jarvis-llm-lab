У меня есть Python-сервис на FastAPI, который принимает POST /events и пишет события в PostgreSQL. Нужно добавить идемпотентность по заголовку X-Idempotency-Key.

Опиши решение так, как если бы ты делал это в проде:
1. схему таблиц или изменения в БД,
2. логику обработки запроса,
3. как избежать race condition при параллельных запросах,
4. что возвращать при повторном запросе с тем же ключом,
5. какие edge cases и ограничения учесть,
6. пример кода на FastAPI + SQLAlchemy.

Не пиши общие слова. Дай конкретный, practically usable ответ.
